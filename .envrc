# ═══════════════════════════════════════════════════════════
# 🌲 FAELIGHT FOREST DOTFILES - .envrc
# Version 2.9.0 - SECURITY COMPLIANT
# ═══════════════════════════════════════════════════════════

# ✅ SAFE: Project identification (simple exports only)
export PROJECT_NAME="0-core"
export PROJECT_VERSION="2.9.0"
export OMARCHY_VERSION="3.2.2"

# ✅ SAFE: Development paths (local directories only)
PATH_add ./scripts
PATH_add ./automation
PATH_add ./.local/bin

# ✅ SAFE: Editor preferences
export EDITOR=nvim
export VISUAL=nvim

# ✅ SAFE: Git configuration helpers
export GIT_AUTHOR_NAME="christian"
export GIT_COMMITTER_NAME="christian"

# ✅ SAFE: Development mode indicators
export DOTFILES_DEV=true
export OMARCHY_ENV="development"

# ═══════════════════════════════════════════════════════════
# 🛡️ SECURITY NOTES:
# - No network calls (no curl/wget)
# - No eval or source of external scripts
# - Only simple exports and PATH additions
# - All paths are local to this repo
# - REVIEWED: 2025-12-13
# ═══════════════════════════════════════════════════════════
