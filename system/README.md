# 🛠️ System Configuration Files

## Snapper Configuration

`snapper-root.conf` - BTRFS snapshot configuration

To restore:
```bash
sudo cp snapper-root.conf /etc/snapper/configs/root
```

## Crontab

`crontab` - Scheduled tasks for auto-sync

To restore:
```bash
crontab crontab
```
